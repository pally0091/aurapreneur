/* eslint-disable no-unused-vars */
import React from 'react';
import Buttons from './Buttons';

const Home = () => {
    return (
        <div>
            <Buttons></Buttons>
        </div>
    );
};

export default Home;